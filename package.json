{
  "name": "@matchmakerjs/docker-image-cli",
  "version": "0.0.1",
  "description": "Build docker image without Dockerfile",
  "main": "dist/cli.js",
  "files": [
    "dist/**/*"
  ],
  "bin": {
    "docker-image": "dist/cli.js"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf -rf dist && tsc"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/matchmakerjs/docker-image-cli.git"
  },
  "keywords": [
    "docker",
    "image"
  ],
  "author": "Olaleye Afolabi",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/matchmakerjs/docker-image-cli/issues"
  },
  "homepage": "https://github.com/matchmakerjs/docker-image-cli#readme",
  "dependencies": {
    "shelljs": "^0.8.4",
    "yargs": "^16.2.0"
  },
  "devDependencies": {
    "@types/node": "^14.14.35",
    "@types/shelljs": "^0.8.8",
    "@types/yargs": "^16.0.0",
    "rimraf": "^3.0.2",
    "ts-node": "^9.1.1",
    "typescript": "^4.2.3"
  }
}
